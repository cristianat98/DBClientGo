package mongo

const (
	clientNotConnected      = "Client is not connected"
	documentNotFoundMessage = "Document not found"
	mongoDB                 = "MongoDB"
	timeoutMessage          = "Invalid timeout: %d. It must be higher than 0"
)
